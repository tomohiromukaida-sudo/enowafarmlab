# Google Tag Manager container ID. Leave unset to disable GTM entirely
# (src/components/summer/GtmScript.tsx no-ops without it).
NEXT_PUBLIC_GTM_ID=

# Absolute site URL, used to resolve OGP/canonical links. Update once the
# LP's real production domain is known.
NEXT_PUBLIC_SITE_URL=https://www.enowa-yufuin.jp
